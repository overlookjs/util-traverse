/* --------------------
 * @overlook/util-traverse module
 * Tests
 * ------------------*/

'use strict';

// Modules
const traverse = require('@overlook/util-traverse');

// Init
require('./support/index.js');

// Tests

describe('tests', () => {
	it.skip('all', () => { // eslint-disable-line jest/no-disabled-tests
		expect(traverse).not.toBeUndefined();
	});
});
